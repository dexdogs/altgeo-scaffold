# ---- backend selection ----
# csv (default, zero-config) | supabase | postgres
DB_BACKEND=csv

# ---- Mapbox (front-end map + reverse geocoding) ----
NEXT_PUBLIC_MAPBOX_TOKEN=

# ---- Supabase (only if DB_BACKEND=supabase) ----
SUPABASE_URL=
SUPABASE_ANON_KEY=

# ---- generic Postgres (only if DB_BACKEND=postgres) ----
DATABASE_URL=
